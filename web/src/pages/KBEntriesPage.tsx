export function KBEntriesPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-2xl font-semibold tracking-tight">KB Entries</h1>
      <p className="text-sm text-muted-foreground">
        The CRUD table lands in Phase C. For now, this confirms the auth flow works and
        the empty admin shell is reachable end-to-end.
      </p>
    </div>
  );
}
